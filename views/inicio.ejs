<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Vesticol Colombia • Moda de hombre y mujer</title>
  <link rel="icon" href="logo.jpg" type="image/jpg">

  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
    }

    nav {
      background-color: #0A0A0A;
      overflow: hidden;
    }

    nav ul {
      margin: 0;
      padding: 0;
      list-style: none;
      display: flex;
      align-items: center;
    }

    nav ul li {
      position: relative;
    }

    nav ul li a {
      display: block;
      padding: 14px 20px;
      text-decoration: none;
      color: white;
    }

    nav ul li a:hover {
      background-color: #575757;
    }

    nav ul li.logo {
      margin-right: auto;
    }

    nav ul li.logo img {
      height: 80px;
      margin: 5px 15px;
    }

    .carrusel {
      position: relative;
      width: 100%;
      max-width: 900px;
      margin: 20px auto;
      overflow: hidden;
      border-radius: 10px;
    }

    .carrusel-contenedor {
      display: flex;
      transition: transform 0.8s ease-in-out; 
      width: 100%;
    }

    .carrusel img {
      width: 100%;
      height: 500px; 
      object-fit: cover;
      flex-shrink: 0; 
      border-radius: 10px;
    }

    .flecha {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(0,0,0,0.5);
      color: white;
      border: none;
      padding: 10px;
      cursor: pointer;
      border-radius: 50%;
      font-size: 24px;
      z-index: 2;
    }

    .flecha.izquierda { left: 10px; }
    .flecha.derecha { right: 10px; }

    
    .categorias {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-wrap: wrap;
      gap: 20px;
      margin: 30px auto;
      max-width: 1100px;
    }

    .categoria {
      position: relative;
      width: 250px;
      height: 250px; 
      overflow: hidden;
      border-radius: 12px;
      cursor: pointer;
      flex-shrink: 0;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .categoria:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 15px rgba(0,0,0,0.3);
    }

    .categoria img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 12px;
      transition: transform 0.3s;
    }

    .categoria span {
      position: absolute;
      bottom: 10px;
      left: 50%;
      transform: translateX(-50%);
      background: rgba(0, 0, 0, 0.6);
      color: white;
      font-size: 20px;
      font-weight: bold;
      padding: 5px 15px;
      border-radius: 5px;
    }

    footer {
      background-color: #000;
      color: #fff;
      text-align: center;
      padding: 15px 0;
      font-family: Arial, sans-serif;
      margin-top: 40px;
    }
  </style>
</head>
<body>

  <nav>
    <ul>
      <li class="logo">
        <a href="/">
          <img src="/logo.jpg" alt="Logotipo Vesticol">
        </a>
      </li>

      <li><a href="/sesionhombre">Hombre</a></li>
      <li><a href="/sesionmujer">Mujer</a></li>
      <li><a href="/carrito">🛒 Carrito</a></li>
      <li><a href="/atencionalcliente">🎧Ayuda</a></li>
      <% if (!datos || !datos.login) { %>
      <li><a href="/ingresar">Registrar</a></li>
      <li><a href="/ingresar/iniciarsesion">Iniciar Sesión</a></li>
      <% } else { %>
      <li><a href="/perfil">Bienvenido, <%= datos.usuario %></a></li>
      <form action="/cerrarsesion" method="POST" style="display:inline;">
        <button type="submit" style="background:none;border:none;color:white;cursor:pointer;padding:14px 20px;font-size:16px;">Cerrar Sesión</button>
      </form>
      <% } %>
    </ul>
  </nav>

  <div class="carrusel">
    <div class="carrusel-contenedor" id="slider">
      <img src="/invictaprodrivernegro.jpg" alt="Imagen 1 - 1920x1080">
      <img src="/niketravisscott (1).jpg" alt="Imagen 2 - 1920x1080">
      <img src="/diorsauvage.jpg" alt="Imagen 3 - 1920x1080">
    </div>

    <button class="flecha izquierda" onclick="cambiarImagen(-1)">&#10094;</button>
    <button class="flecha derecha" onclick="cambiarImagen(1)">&#10095;</button>
  </div>

  <div class="categorias">
    <a href="/sesionhombre" class="categoria">
      <span>Hombre</span>
      <img src="/hombre.jpg" alt="Categoría Hombre">
    </a>

    <a href="/sesionmujer" class="categoria">
      <span>Mujer</span>
      <img src="/mujer.jpg" alt="Categoría Mujer">
    </a>

    <a href="/carrito" class="categoria">
      <span>Carrito</span>
      <img src="/carritodecompras.jpg" alt="Carrito de compras">
    </a>

    <a href="/atencionalcliente" class="categoria">
      <span>Ayuda</span>
      <img src="/atencionalcliente.jpg" alt="Atención al cliente">
    </a>
  </div>

  <footer>
    <p>© 2025 Vesticol Colombia — Todos los derechos reservados.</p>
  </footer>

  <script>
    const slider = document.getElementById("slider");
    const imgs = document.querySelectorAll(".carrusel img");
    let i = 0;
    let interval = setInterval(autoPlay, 3000);

    function mostrarImagen(n) {
      i = (n + imgs.length) % imgs.length;
      slider.style.transform = `translateX(-${i * 100}%)`;
    }

    function cambiarImagen(direccion) {
      mostrarImagen(i + direccion);
      reiniciarAutoPlay();
    }

    function autoPlay() {
      mostrarImagen(i + 1);
    }

    function reiniciarAutoPlay() {
      clearInterval(interval);
      interval = setInterval(autoPlay, 3000);
    }
  </script>

</body>
</html>
